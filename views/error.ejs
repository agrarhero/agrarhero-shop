<%- include('partials/header') %>
<section class="block">
  <div class="wrap" style="text-align:center;padding:60px 0">
    <div style="font-family:'Barlow Condensed';font-size:5rem;font-weight:700;color:var(--accent);line-height:1"><%= code %></div>
    <h1 style="font-size:2rem;margin:10px 0"><%= title %></h1>
    <p style="color:var(--muted);margin-bottom:24px"><%= message %></p>
    <a class="btn btn-accent" href="/">Zur Startseite</a>
  </div>
</section>
<%- include('partials/footer') %>
